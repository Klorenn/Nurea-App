[package]
name = "nurea-escrow"
version = "0.1.0"
description = "Escrow para citas médicas Nurea (Soroban)"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "25.3.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = "symbols"
