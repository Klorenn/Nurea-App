// Componente deprecado: reemplazado por el rediseño inline de /login/page.tsx.
// Se mantiene un stub vacío para no romper imports históricos.
// Acepta props arbitrarias para compatibilidad con llamadas legacy
// (backdropBlurAmount, color, etc.) sin romper el typecheck.
export function SmokeyBackground(_props?: Record<string, unknown>) {
  return null
}
export function LoginForm(_props?: Record<string, unknown>) {
  return null
}
